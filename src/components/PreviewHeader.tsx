import { WindowHeader } from "@/components/styled/WindowHeader";

export default function PreviewHeader() {
    return (
        <WindowHeader>
            Preview Header
        </WindowHeader>
    )
}